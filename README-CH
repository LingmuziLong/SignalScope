

# SignalScope - 多通道信号实时可视化系统

[![Python Version](https://img.shields.io/badge/python-3.9+-blue.svg)](https://www.python.org/)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.104+-green.svg)](https://fastapi.tiangolo.com/)
[![License](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)



## 1. 项目简介

**SignalScope** 是一个面向可配置多通道信号的实时可视化系统。  
通过 WebSocket 实时推送多路信号数据，并在浏览器中以波形图、频谱图、直方图形式呈现，支持自定义通道数量、布局、轮播规则及数据源。

​       **应用场景**：

- 射电天文望远镜的多波束监控（

- 业余无线电或 SDR 的多频率信号监测

- 实验室仪器通道数据的实时展示

	
	
	**核心功能**：

- 动态节点数量（1~100 个通道）

- 双节点上下布局自动轮播（可调轮询间隔1-60s）

- 手动切换节点对，支持暂停/播放

- 时域波形、频域频谱、幅度直方图三视图

- 实时统计信息（RMS、峰值、主频）

- 节点名称自定义（永久保存）

- 数据源切换（模拟 / 真实硬件预留）

- 一键反色模式（夜间模式）

- 中英文双语界面

- 图表点击显示精确数值

	

## 2. 系统架构

```text
[浏览器] ←── WebSocket ──→ [FastAPI 后端]
    │                         │
    │                         ├─ 模拟数据源 (内置)
    │                         ├─ 真实数据源 (预留接口)
    │                         └─ 文件/流数据 (可扩展)
    │
    └─ Canvas 绘图 (时域/频域/直方图)
```

- **后端**：FastAPI + WebSocket + NumPy (异步并发)
- **前端**：原生 JavaScript + Canvas (无第三方库，高性能绘图)
- **数据源抽象**：统一接口 `generate_node_data(node_idx, node_type)`，支持模拟信号和真实硬件无缝切换



## 3. 环境要求

- Python 3.9+
- 依赖包：fastapi, uvicorn, numpy
- 浏览器：Chrome / Firefox / Edge（支持 WebSocket）



## 4. 安装与运行

**GitHub 仓库**：[https://github.com/LingmuziLong/SignalScope](https://github.com/LingmuziLong/SignalScope)

```bash
# HTTPS（推荐，无需配置密钥）
git clone https://github.com/LingmuziLong/SignalScope.git

# SSH（需先配置 SSH 公钥）
git clone git@github.com:LingmuziLong/SignalScope.git

# 创建虚拟环境（可选）
python -m venv venv
source venv/bin/activate   # Linux/Mac
# 或 venv\Scripts\activate   # Windows

# 安装依赖
pip install -r requirements.txt

# 启动后端
python backend/main.py
```

- 默认访问地址：`http://localhost:8000`
- 如需外网访问，修改 `config.py` 中的 `host` 为 `0.0.0.0`



## 5. 使用说明

#### 5.1 节点轮播与手动控制
- 页面自动每 10 秒切换下一对节点（1&2 → 3&4 → … → 19&20 → 循环）
- 点击 ◀ ▶ 按钮手动切换，点击“暂停”停止自动轮播，“播放”恢复
- 轮播时间可在右侧输入框设置（1~60 秒），点击“保存”立即生效

#### 5.2 节点数量设置
- 在控制栏右侧“节点数量”输入框中修改数量（1~100），点击“设置”后后端立即调整数据生成量，前端动态重建界面
- 当节点数为奇数时，最后一页只显示单个节点（第二个卡片为空，保留坐标系）

#### 5.3 节点名称修改
- **左键点击**节点名称标签，弹出对话框输入自定义名称
- 名称自动保存到浏览器本地存储，刷新页面后依然有效

#### 5.4 数据源切换
- 点击“更换节点”按钮，可选择“模拟数据节点（代码生成）”或“真实数据节点（硬件预留）”
- 真实数据节点当前返回全零数据（占位），用户需按第 6 节进行扩展

#### 5.5 图表交互
- **点击时域波形或频谱图曲线**：弹出该点的采样点/频率值和幅度
- **点击直方图柱**：弹出幅度区间、计数和百分比

#### 5.6 反色模式
- 点击左上角 ☾ 按钮，整个界面（包括图表线条）反色，适合夜间或投影环境
- 再次点击恢复

#### 5.7 语言切换
- 点击“Ch”按钮，界面可在中文和英文之间切换，所有文本、按钮、坐标轴标签同步翻译

	

## 6. 真实数据接入接口（重点）

项目已预留**完全解耦的数据接入层**，用户无需修改前端或其他后端逻辑，只需实现一个函数即可接入任何真实数据源（SDR、射电望远镜基带、文件回放等）。

#### 6.1 接口位置
文件：`backend/signal_generator.py`  
方法：`generate_real_data_signals(self, node_idx: int, n: int) -> Dict[str, Any]`

#### 6.2 接口规范
- **输入参数**：
  - `node_idx`：当前节点索引（0 ~ NODE_COUNT-1）
  - `n`：需要返回的采样点数（由配置 `SIGNAL_CONFIG["samples_per_node"]` 决定，默认 500）
- **返回值**：字典，必须包含两个键：
  ```python
  {
      "samples": [float],   # 长度为 n 的浮点数列表，代表信号幅度
      "node_idx": int       # 原样返回 node_idx
  }
  ```
- **注意事项**：返回的采样点应已做限幅（建议范围 -100 ~ 100，与配置中 `max_amplitude/min_amplitude` 对应），否则前端绘图可能超出坐标轴范围。

实现上述函数后，用户在 Web 界面点击“更换节点” → 选择“真实数据节点”，系统即会自动调用该函数获取真实数据并实时刷新图表，无需修改前端代码。



## 7. 后续硬件测试与优化计划

为了将 SignalScope 真正部署到实际观测或监测环境中，后续将会使用真实硬件进行测试。



## 8. 配置文件说明（`config.py`）

| 配置项          | 说明                                             |
| --------------- | ------------------------------------------------ |
| `SERVER_CONFIG` | 服务器主机、端口、调试模式                       |
| `SIGNAL_CONFIG` | 节点数量、每节点采样点数、更新间隔(秒)、幅度范围 |
| `NODE_TYPES`    | 数据源类型名称（用于前端显示和后端判断）         |



## 9. 常见问题

**WebSocket 连接失败**  
- 检查后端是否运行，防火墙是否开放 8000 端口
- 查看浏览器控制台是否报错

**真实数据无波形**  
- 确认 `generate_real_data_signals` 返回值格式正确，采样点数等于 `n`
- 检查数据是否超出 `max_amplitude/min_amplitude` 范围，可适当调整配置或增加限幅

**画布不显示或模糊**  
- 刷新页面，确保浏览器支持 Canvas
- 若高分屏模糊，已通过 HiDPI 适配自动处理，可尝试缩放页面

**切换节点数量后图表不更新**  
- 前端会自动重建卡片，若未生效请清除浏览器缓存

	

## 10. 作者信息

- 开发者：LingmuziLong

- 联系方式：lingmuzi.long@outlook.com

- 最后更新：2026年5月

	

## 11. 开源协议

本项目采用 **MIT 许可证**，欢迎二次开发和商用。若将 SignalScope 用于学术项目或硬件联调，请引用本仓库。



## 12. 致谢

本项目为中国科学院国家天文台大学生创新实践训练计划（简称“科创计划”）的延伸成果。感谢中国科学院国家天文台提供的宝贵指导与支持，感谢课题组师生的帮助与鼓励。
